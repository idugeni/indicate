node_modules/
.next/
coverage/
playwright-report/
test-results/
.release-evidence/
semantic-review/
.env
.env.local
.env.*.local
.env.production
*.tsbuildinfo
.DS_Store

# Cache index CodeGraph (18 MB, regenerable via `codegraph init`)
.codegraph/

# Konfig personal AI-agent CLI (untracked, per-mesin)
.codex/
.gemini/
.claude/

# Bundle skill AI pihak ketiga (reproducible via skills-lock.json, 25 entri).
# Skill milik repo tetap ter-track via negasi.
.agents/skills/*
!.agents/skills/indicate-conventions/
!.agents/skills/tenant-onboarding/
!.agents/skills/agent-discipline/
# 7 skill desain lokal opsional TANPA pin (sengaja: banner-design, brand,
# design, design-system, slides, ui-styling, ui-ux-pro-max). Berfungsi lokal,
# tidak ikut fresh clone. Jangan hapus, jangan tulis hash manual ke
# skills-lock.json — untuk mem-pin salah satunya, pakai tool yang
# menghasilkan lock (npx skills add <source>), bukan edit tangan.
.kiro/skills/*

# Settings IDE per-mesin (.mcp.json di root tetap shared)
.vscode/
.kiro/settings/

# State sementara Supabase CLI (ref proyek, versi, URL pooler)
supabase/.temp/

# Backup env berisi secret (pernah ada .env.seoul-bak berisi kredensial live)
*.bak
*.seoul-bak
.env*.bak

.vercel

# Sabuk ganda: tolak semua varian .env, kecuali kontrak contoh
.env*
!.env.example
