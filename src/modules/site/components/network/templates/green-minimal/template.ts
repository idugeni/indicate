/**
 * Identitas template Green Minimal (hijau natural).
 *
 * @remarks
 * Satu dari sepuluh template tenant. Dispatcher `network-listing` switch ke id ini; fallback `clean-blue`.
 */
export const GREEN_MINIMAL_TEMPLATE_ID = 'green-minimal' as const;

export type GreenMinimalTemplateId = typeof GREEN_MINIMAL_TEMPLATE_ID;
