# Stage 7 production-readiness release gate and migrated acceptance contract

Stage 7 adds a fail-closed production-readiness command, configuration-derived three-root/three-region acceptance coverage, schema-14 migration integrity, provider inspection, promotion evidence, and rollback validation. The final PostgreSQL blocker is closed: the live suite derives the expected schema-14 version, name, and checksum from the unique `REVIEWED_MIGRATION_MANIFEST` entry instead of retaining a digest literal. The complete tracked and untracked workspace keeps the prior release, persistence, isolation, provider, scanner, and migration remediations intact. **Watch for:** No actionable findings remain; the PostgreSQL 17 suite is wired into CI, although this review environment’s container runtime failed before a local PostgreSQL instance could start.

**Verdict**: APPROVED

## High-level view

Migration readiness binds each of the 14 journal entries to an ordered version/name/tag record and full-file SHA-256 digest. Canonicalization changes only the exact self-referential checksum literal, so executable SQL before or after registration remains covered and malformed registrations fail closed.

The production acceptance boundary spans all nine regional scenarios through migrated Drizzle Stage 4, Stage 5, and Stage 6 repositories under `indicate_runtime`. Direct forced-RLS table reads remain closed, while a bounded exact-host security-definer function exposes only coherent active mappings for the configured 12 hosts.

Readiness combines complete applied-migration comparison with Supabase, Upstash, Telegram, Cloudflare, Vercel, HTTPS, and both R2 trust planes. Provider failures become stable categories, Telegram secret verification is non-mutating and dedicated-capacity, and reports omit raw credentials and provider errors.

Release evidence requires all 15 readiness checks, 11 quality stages, and nine acceptance scenarios to be complete and internally consistent. Evidence cleanup is confined below `.stage7-evidence`, rollback requires all seven durable recovery families, and scanner fixture allowances are content-bound rather than directory-wide.

<details>
<summary>Issues (0)</summary>

No actionable findings.

</details>

<details><summary>Details</summary>

## Schema-14 assertion follows the reviewed manifest

`tests/integration/stage7-postgres-live.integration.test.ts` filters `REVIEWED_MIGRATION_MANIFEST` for version 14, requires exactly one match, and parameterizes the database assertion with that entry’s version, name, and checksum. The obsolete `sha256:ab38…77c0` value appears only in the prior review artifact; executable code uses the current `sha256:81a8…91a3` manifest entry.

The Stage 2 live setup applies both Stage 7 migrations before `test:postgres:live` reaches Stage 7. That command chains the Stage 2/4/5/6/7 suites, and CI provisions `postgres:17-alpine` with `TEST_DATABASE_URL`, so the assertion targets the row inserted by migration 0013.

## Complete migration bodies are cryptographically bound

The manifest reader requires 14 ordered journal records and recomputes every SQL digest. Canonicalization normalizes CRLF and replaces only the checksum literal in the unique self-registration row; migration 0000 is hashed in full, while missing, duplicate, malformed, commented-out, or structurally unexpected registrations are rejected.

Regression coverage changes marker comments, swaps file identities, and appends executable SQL after registration. A review-time calculation also returned 14 matching digests and exactly one schema-14 entry. Migration 0013 rewrites and verifies the first 13 applied records before inserting its own digest, and readiness compares the complete ordered applied sequence rather than `max(version)`.

## Migrated 3x3 journey preserves tenant and RLS boundaries

The live contract inserts nine globally distinct region IDs, publishes through `DrizzleStage4Repository` and the worker, reads through `DrizzleStage5Repository`, authenticates through `DrizzleStage6Repository`, and observes public records from the same migrated database. The deterministic matrix adds media, SEO, analytics, Telegram, audit, cache, and credential-isolation coverage across all nine scenarios.

The discovery function caps already-normalized exact hosts at 100, fixes its search path, fully qualifies tenant tables, revokes public access, and grants execution only to `indicate_runtime`. The live contract requires forced-RLS direct reads to return zero, all 12 coherent mappings to resolve through the function, and inactive regions to disappear. Foreign cache data is rejected, cross-tenant media and job access are denied, and foreign job lookup matches absent-identifier denial.

## Provider readiness covers both management and data planes

Cloudflare checks exact root authority, proxied routes, and Full (strict); Vercel checks all 12 exact associations; HTTPS requires Cloudflare-fronted responses. R2 requires management-plane bucket/privacy state plus S3 `HeadBucket` with the media credentials, whose production values are length- and placeholder-bounded.

Telegram combines Bot API health, exact webhook URL, and one authenticated deployment challenge. The challenge requires Cloudflare origin proof, uses a dedicated fail-closed limiter, performs constant-time accepted and deterministic-mismatch comparisons in one request, and returns no secret-derived output. Tests cover allowance one, repetition, mismatch, edge proof, limiter outage, unavailable providers, parent-zone rejection, R2 command shape, and report sanitization.

## Promotion evidence and rollback remain fail closed

Evidence parsers require exact keys, unique exact check/stage sets, schema 14, passed categories, nine scenarios, and no contradictory failures. The release command persists gate, acceptance, and readiness evidence before deriving promotion; unavailable or malformed evidence adds an explicit failure.

Evidence paths must stay relative beneath `.stage7-evidence`, reject traversal and symlink components, and remove only four named artifacts. Rollback requires schema compatibility, one-project topology, Cloudflare authority, and activation, cleanup, transition-receipt, lease, invalidation, queue, and webhook-outcome recovery; omitting any family blocks rollback.

## Secret scanning and verification results

The scanner covers executable sources and common environment, shell, documentation, key, key-store, Terraform, and configuration formats. Synthetic negatives are exempt only when exact path, SHA-256 digest, and sentinel marker match; altered fixtures, added files, production-looking credentials, configured secret values, and transient logs fail.

`npm run test:stage7` passed configuration, typechecking, lint/policies, 87 unit tests, 34 property tests, 85 non-database integration tests, secure build, 27 Playwright tests, production audit, and diff checking. Tracked and untracked whitespace checks also passed. The sandbox container runtime exited before local PostgreSQL initialization, so migration order, the manifest-derived assertion, runtime role, suite chain, PostgreSQL 17 service, and `TEST_DATABASE_URL` wiring were verified from code and workflow configuration.

</details>

<details>
<summary>File map</summary>

- `.env.example` — marks database passwords as replacement values.
- `.github/workflows/stage2-quality-gate.yml` — runs Stage 7 and PostgreSQL 17 contracts.
- `.gitignore` — excludes generated Stage 7 evidence.
- `app/api/webhooks/readiness/telegram-secret/route.ts` — implements the atomic Telegram-secret challenge.
- `app/stage5-composition.ts` — derives test public fixtures from runtime roots.
- `docs/PRODUCTION_READINESS_RUNBOOK.md` — documents schema-14 release and rollback.
- `drizzle/0012_stage7_readiness_discovery.sql` — adds bounded RLS-safe discovery.
- `drizzle/0013_stage7_migration_body_digests.sql` — records full-body digests and schema 14.
- `drizzle/meta/_journal.json` — registers migrations 13 and 14.
- `package.json` — adds Stage 7 policy, release, readiness, and live commands.
- `scripts/check-migration-policy.mjs` and `scripts/check-stage7-policy.mjs` — enforce migration, topology, host, artifact, and secret contracts.
- `scripts/decide-stage7-promotion.ts`, `scripts/run-stage7-gate.mjs`, `scripts/run-stage7-release.mjs`, `scripts/stage7-evidence-directory.mjs`, `scripts/validate-production-readiness.ts` — collect, validate, and confine release evidence.
- `src/application/deployment/schema-gate.ts` — raises the required schema to 14.
- `src/application/stage6/trusted-request-boundary.ts` — exposes constant-time secret comparison.
- `src/application/stage7/**` and `src/domain/stage7/**` — define and evaluate acceptance, readiness, evidence, migration, promotion, and rollback contracts.
- `src/config/schema.ts` — bounds production secrets and R2 credentials.
- `src/infrastructure/deployment/production-readiness-adapter.ts` — performs migration, database, provider, and transport probes.
- `src/infrastructure/storage/r2-object-storage.ts` — adds S3 `HeadBucket` health.
- `src/infrastructure/testing/stage5-fixture.ts` — supplies expanded public and shared-journey fixtures.
- `src/infrastructure/cloudflare/cloudflare-authority.ts` and `src/infrastructure/vercel/exact-domain-adapter.ts` — add exact read-only provider inspection.
- `src/ports/production-readiness.ts` — defines readiness snapshots.
- `tests/e2e/**`, `tests/integration/**`, and `tests/unit/**` — cover the public matrix, policies, providers, migrated persistence, rollback, migration integrity, cleanup, and Telegram readiness.
- `tests/fixtures/policy/stage7/**` — provides content-bound scanner fixtures.
- `semantic-review/2026-08-31-003025-pr-0.md` through `semantic-review/2026-08-31-022923-pr-0.md` — preserve prior findings and closure history.

Full change reference: `git diff HEAD` plus every path from `git ls-files --others --exclude-standard`.

</details>
