# Stage 6 external-entrypoint hardening with complete fresh bootstrap

This follow-up completes the fresh-database path for the Stage 6 security hardening by adding migration `0010_stage6_security_hardening.sql` to the ordered PostgreSQL bootstrap. The full local change addresses the original eight findings and the later replay-fencing and platform-actor-impersonation findings while preserving shared CMS/Telegram services and client-secret boundaries. The current Stage 6 gate passes end to end in the available environment. **Watch for: no confirmed actionable issues; confirmed validation limitation — the PostgreSQL 17 runtime contracts remain unexecuted because this sandbox has no reachable fresh database endpoint.**

**Verdict**: APPROVED

## High-level view

Tenant roles can grant only same-organization permissions, while platform customer authority is isolated in owner-provisioned grants and revalidated against both the transaction actor and verified authentication identity. Exact API and webhook hosts, Cloudflare origin proof, and a trusted-source limiter before API-key lookup close the original trust-boundary and unauthenticated-work gaps.

Telegram claims replay identity before tenant resolution, rotates a UUID fence on lease reclaim, and records successful shared-service receipts in the business transaction. Prepared success and rejection outcomes are recoverable by duplicates, rejected guidance is delivered consistently, and stale workers cannot commit the covered Article, Site-assignment, Media-activation, or Publication mutations.

The fresh Stage 2 bootstrap now applies migrations `0000` through `0010` before the later runtime-role suites consume the shared database. Schema version 11, migration policy, type checking, dependency audit, the complete Stage 6 gate, and diff whitespace checks pass; only environment-dependent PostgreSQL execution is unavailable.

<details>
<summary>Issues (0)</summary>

No confirmed actionable issues.

</details>

<details><summary>Details</summary>

## Fresh bootstrap reaches the Stage 6 hardening contract

The previous review's sole finding is fixed at `tests/integration/stage2-postgres-live.integration.test.ts:20`: `migrationFiles` ends with `drizzle/0009_stage6_external_entrypoints.sql` followed by `drizzle/0010_stage6_security_hardening.sql`. The bootstrap executes the array sequentially before fixture insertion (`tests/integration/stage2-postgres-live.integration.test.ts:113-118`), and migration 0010 records schema version 11 (`drizzle/0010_stage6_security_hardening.sql:365-366`), matching `REQUIRED_SCHEMA_VERSION` and the Stage 6 live-suite precondition.

`npm run test:stage6` passed configuration validation, type checking, all policies, 62 unit tests, 34 property tests, 60 integration tests with 39 environment-dependent skips, the secure build and secret scan, and 25 Playwright tests. `npm audit --omit=dev` found zero vulnerabilities, and `git diff --check HEAD` passed. The Stage 2 and Stage 6 PostgreSQL commands discovered 15 and 4 live tests respectively, but all 19 skipped because `TEST_DATABASE_URL` is unset; Podman reports stale containers whose process namespace is missing and whose mapped ports refuse or time out, so a fresh PostgreSQL 17 combined run was unavailable.

</details>

<details>
<summary>File map</summary>

- `tests/integration/stage2-postgres-live.integration.test.ts` — applies migration 0010 after 0009 during fresh combined-contract bootstrap.
- `drizzle/0010_stage6_security_hardening.sql` — isolates platform grants and adds replay leases, fencing, durable outcomes, and transactional business receipts.
- `tests/integration/stage6-postgres-live.integration.test.ts` — specifies platform-impersonation, tenant-escalation, replay-fencing, receipt, and runtime-role contracts.
- `src/application/stage6/telegram-workflow-service.ts` and `src/application/stage6/webhook-service.ts` — reconcile durable replay outcomes without rerunning completed work.
- `src/application/stage6/trusted-request-boundary.ts`, `proxy.ts`, and external routes — enforce exact authority, origin proof, and pre-authentication limiting.
- Stage 6 repositories and tests — separate platform authority, preserve shared-service parity, audit denials, and cover adversarial boundaries.

Full local diff: `git diff HEAD` for tracked changes plus `git ls-files --others --exclude-standard` for untracked files.

</details>
