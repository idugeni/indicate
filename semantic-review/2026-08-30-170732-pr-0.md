# Journal-backed Stage 2 authorization hardening clearance

The deployment-path remediation registers migration `0003_stage2_authorization_hardening` in Drizzle’s production journal and makes migration policy validation cross-check every SQL artifact against that journal. This closes the prior gap where tests installed the reciprocal Telegram/Membership lock while `db:migrate` silently omitted it. The complete Stage 2 gate passes with 34 unit, 5 property, 15 deterministic integration, a secure production build, and 7 Playwright tests; the 11 PostgreSQL cases and one live Supabase contract remain externally gated, with PostgreSQL mandatory in CI. **Watch for:** no confirmed blocking concern remains; **confirmed** the 11 live PostgreSQL cases are skipped without `TEST_DATABASE_URL`, but the Stage 2 workflow supplies an ephemeral PostgreSQL 17 service and test-only URL unconditionally.

**Verdict**: APPROVED

## High-level view

The production migrator and policy gate now share the same source of migration discovery. Journal index 3 resolves the hardening SQL through Drizzle, while the policy rejects an SQL migration missing from the journal and rejects journal/SQL count drift.

The hardening migration installs the shared Membership-row serialization point previously proven by the concurrent PostgreSQL regression. The schema readiness gate requires version 4, so application activation cannot treat the pre-hardening schema as current.

The deterministic gate covers journal presence, source-level locking requirements, authorization behavior, seed atomicity, secure build output, and browser organization isolation. CI adds the production-adapter PostgreSQL matrix against PostgreSQL 17, including concurrency, RLS/grants, persisted actor revalidation, relation constraints, audit rollback, and seed rollback.

<details>
<summary>Issues (0)</summary>

No actionable concerns remain.

</details>

<details><summary>Details</summary>

## Production migration discovery now includes authorization hardening

The migration policy parses the journal, derives each SQL filename’s tag, requires every tag to be discoverable, and requires equal journal and SQL counts. Together these checks prevent both unjournaled SQL artifacts and journal-only artifacts from passing the release gate. The deterministic integration suite pins the specific regression by requiring the `0003` tag, while the migration records schema version 4 and the readiness gate requires the same version.

## Checkpoint 6 validation spans deterministic and live trust boundaries

`npm run test:stage2` completed successfully in this review: validated configuration, typechecking, zero-warning lint and policy gates, 34 unit tests, 5 fast-check properties, 15 deterministic integration tests, a production build plus client-secret scan, and 7 non-interactive Playwright cases. Twelve external-contract cases were skipped locally: 11 PostgreSQL tests because no `TEST_DATABASE_URL` was supplied and one opt-in live Supabase Auth probe.

The `live-postgres-contract` CI job starts `postgres:17-alpine` and passes a test-only `TEST_DATABASE_URL` to the complete integration target. It executes the 11 production-adapter cases covering the previously reviewed concurrency and security boundaries without production credentials.

</details>

<details>
<summary>File map</summary>

- `drizzle/meta/_journal.json` — registers the authorization-hardening migration as ordered production migration index 3.
- `scripts/check-migration-policy.mjs` — rejects SQL/journal discovery gaps and count drift.
- `tests/integration/stage2-persistence-auth.integration.test.ts` — pins deterministic coverage to the hardening SQL and journal tag.
- `drizzle/0003_stage2_authorization_hardening.sql` — serializes active Telegram mapping validation on the Membership row and records schema version 4.
- `scripts/migrate.ts`, `src/application/deployment/schema-gate.ts` — run journal-backed migrations and refuse readiness below version 4.
- `tests/integration/stage2-postgres-live.integration.test.ts` — exercises production adapters, concurrent authorization changes, grants/RLS, relation constraints, and rollback behavior.
- `.github/workflows/stage2-quality-gate.yml` — runs the deterministic gate and unconditional PostgreSQL 17 contract job.

Full diff: unavailable because `main` has no commits and the repository remains an untracked initial tree; this pass compares the current filesystem with the Stage 2 specification and `semantic-review/2026-08-30-170506-pr-0.md`.

</details>
